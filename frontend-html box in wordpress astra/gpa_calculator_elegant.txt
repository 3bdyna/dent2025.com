<style>
    /* --------------------------------------------------
       1. BANNER STYLES (Trigger on Homepage)
       -------------------------------------------------- */
    .gpa-banner {
        background: #1e1e1e;
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 14px 20px;
        border-radius: 14px;
        color: #f8fafc;
        font-family: 'Outfit', 'Noto Kufi Arabic', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        display: flex;
        justify-content: space-between;
        align-items: center;
        cursor: pointer;
        direction: rtl;
        transition: all 0.25s ease;
        max-width: 100%;
        margin: 10px 0;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        box-sizing: border-box;
        overflow: hidden;
        user-select: none;
    }
    .gpa-banner:hover {
        background: rgba(255, 255, 255, 0.06);
        border-color: rgba(255, 255, 255, 0.25);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        transform: translateY(-2px);
    }
    .gpa-banner-text {
        flex: 1;
        text-align: right;
    }
    .gpa-banner-text h3 {
        margin: 0;
        font-size: 1.05rem;
        font-weight: 700;
        color: #f8fafc;
    }
    .gpa-banner-text p {
        margin: 3px 0 0 0;
        font-size: 0.82rem;
        color: #9ca3af;
    }
    .gpa-banner-arrow {
        color: #f8fafc;
        font-size: 1.1rem;
        font-weight: bold;
        background: rgba(255, 255, 255, 0.06);
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        transition: all 0.2s ease;
        flex-shrink: 0;
    }
    .gpa-banner:hover .gpa-banner-arrow {
        background: rgba(255, 255, 255, 0.15);
        transform: scale(1.05);
    }

    /* --------------------------------------------------
       2. MODAL CONTAINER & OVERLAY
       -------------------------------------------------- */
    .gpa-modal-overlay {
        position: fixed !important;
        top: 0 !important;
        left: 0 !important;
        right: 0 !important;
        bottom: 0 !important;
        width: 100% !important;
        height: 100% !important;
        background: rgba(0, 0, 0, 0.84) !important;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        z-index: 999999 !important;
        display: none !important;
        justify-content: center;
        align-items: center;
        opacity: 0;
        transition: opacity 0.25s ease;
        padding: 16px;
        box-sizing: border-box;
        margin: 0 !important;
    }
    .gpa-modal-overlay.active {
        display: flex !important;
        opacity: 1;
    }
    .gpa-modal-content {
        background: #141519;
        color: #f8fafc;
        font-family: 'Outfit', 'Noto Kufi Arabic', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        padding: 24px;
        border-radius: 20px;
        direction: rtl;
        width: 100%;
        max-width: 580px;
        max-height: 90vh;
        overflow-y: auto;
        position: relative;
        transform: translateY(16px);
        transition: transform 0.25s cubic-bezier(0.16, 1, 0.3, 1);
        box-shadow: 0 25px 60px rgba(0, 0, 0, 0.75), 0 0 0 1px rgba(255, 255, 255, 0.08);
        box-sizing: border-box;
    }
    .gpa-modal-overlay.active .gpa-modal-content {
        transform: translateY(0);
    }

    /* Close Button */
    .gpa-close-btn {
        position: absolute;
        top: 16px;
        left: 16px;
        background: rgba(255, 255, 255, 0.06);
        border: 1px solid rgba(255, 255, 255, 0.12);
        color: #cbd5e1;
        width: 34px;
        height: 34px;
        border-radius: 10px;
        font-size: 1.3rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s ease;
        line-height: 1;
        padding: 0;
        z-index: 10;
    }
    .gpa-close-btn:hover {
        background: rgba(255, 255, 255, 0.15);
        color: #ffffff;
        transform: scale(1.05);
    }

    /* Modal Titles */
    .gpa-modal-header {
        text-align: center;
        margin-bottom: 18px;
        padding-top: 4px;
    }
    .gpa-modal-title {
        margin: 0 0 4px 0;
        font-size: 1.3rem;
        font-weight: 700;
        color: #f8fafc;
        letter-spacing: -0.01em;
    }
    .gpa-modal-subtitle {
        margin: 0;
        font-size: 0.84rem;
        color: #9ca3af;
        font-weight: 500;
    }

    /* --------------------------------------------------
       3. INTERNAL CARDS & SECTIONS
       -------------------------------------------------- */
    .gpa-card {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.07);
        padding: 16px;
        border-radius: 16px;
        margin-bottom: 14px;
        box-sizing: border-box;
    }
    .gpa-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 12px;
        gap: 10px;
        flex-wrap: wrap;
    }
    .gpa-card-title {
        margin: 0;
        font-size: 0.95rem;
        color: #f1f5f9;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 6px;
    }
    .gpa-hours-badge {
        font-size: 0.76rem;
        font-weight: 600;
        color: #38bdf8;
        background: rgba(56, 189, 248, 0.1);
        border: 1px solid rgba(56, 189, 248, 0.2);
        padding: 2px 8px;
        border-radius: 20px;
        display: inline-flex;
        align-items: center;
    }

    /* Input Fields Universal Reset */
    .gpa-modal-content input, 
    .gpa-modal-content select {
        background: rgba(0, 0, 0, 0.4);
        color: #f8fafc;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 10px;
        font-family: inherit;
        box-sizing: border-box;
        outline: none;
        transition: all 0.2s ease;
        -webkit-appearance: none;
        -moz-appearance: none;
        appearance: none;
    }
    .gpa-modal-content input:focus, 
    .gpa-modal-content select:focus {
        border-color: rgba(56, 189, 248, 0.6);
        background: rgba(0, 0, 0, 0.6);
        box-shadow: 0 0 0 3px rgba(56, 189, 248, 0.15);
    }

    /* Previous GPA Inputs Grid */
    .gpa-prev-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 12px;
    }
    .gpa-field-group {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }
    .gpa-field-label {
        font-size: 0.76rem;
        color: #9ca3af;
        font-weight: 500;
    }
    .gpa-field-input {
        width: 100%;
        padding: 8px 12px;
        height: 38px;
        font-size: 0.88rem;
        text-align: center;
        font-weight: 600;
    }

    /* Toggle Pill (Letter vs Marks) */
    .gpa-toggle-pill {
        display: inline-flex;
        background: rgba(0, 0, 0, 0.45);
        padding: 3px;
        border-radius: 10px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        gap: 2px;
    }
    .gpa-toggle-btn {
        background: transparent;
        border: none;
        color: #9ca3af;
        padding: 5px 12px;
        border-radius: 7px;
        font-size: 0.76rem;
        cursor: pointer;
        font-family: inherit;
        font-weight: 500;
        transition: all 0.2s ease;
    }
    .gpa-toggle-btn.active {
        background: rgba(255, 255, 255, 0.15);
        color: #ffffff;
        font-weight: 600;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }

    /* --------------------------------------------------
       4. SUBJECT ROWS (Modern Adaptive Card Design)
       -------------------------------------------------- */
    #subjects-list {
        display: flex;
        flex-direction: column;
        gap: 8px;
        margin-top: 4px;
    }
    .gpa-subject-item {
        background: rgba(255, 255, 255, 0.025);
        border: 1px solid rgba(255, 255, 255, 0.07);
        border-radius: 12px;
        padding: 10px 14px;
        transition: all 0.2s ease;
        box-sizing: border-box;
    }
    .gpa-subject-item:hover {
        background: rgba(255, 255, 255, 0.045);
        border-color: rgba(255, 255, 255, 0.12);
    }

    /* Desktop / Default Layout */
    @media (min-width: 481px) {
        .gpa-subject-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
        }
        .gpa-subject-top-row {
            display: contents;
        }
        .gpa-mobile-del-wrapper {
            display: none;
        }
        .gpa-desktop-del-wrapper {
            display: flex;
        }
        .gpa-subject-info {
            flex: 1;
            min-width: 0;
        }
        .gpa-subject-controls {
            flex-shrink: 0;
            display: flex;
            align-items: center;
            gap: 8px;
        }
    }

    /* Mobile Layout (< 480px) */
    @media (max-width: 480px) {
        .gpa-subject-item {
            display: flex;
            flex-direction: column;
            align-items: stretch;
            gap: 10px;
            padding: 12px;
        }
        .gpa-subject-top-row {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 10px;
            width: 100%;
        }
        .gpa-subject-info {
            flex: 1;
            min-width: 0;
        }
        .gpa-mobile-del-wrapper {
            display: flex;
            flex-shrink: 0;
        }
        .gpa-desktop-del-wrapper {
            display: none;
        }
        .gpa-subject-controls {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8px;
            width: 100%;
            border-top: 1px solid rgba(255, 255, 255, 0.06);
            padding-top: 8px;
        }
        .gpa-hours-wrapper {
            flex: 1;
        }
        .gpa-grade-wrapper {
            flex: 1;
        }
    }

    /* Subject Info & Typography */
    .gpa-subject-name {
        margin: 0;
        font-size: 0.88rem;
        font-weight: 500;
        color: #f1f5f9;
        line-height: 1.4;
        word-break: break-word;
        direction: auto;
        unicode-bidi: plaintext;
        text-align: start;
    }
    .gpa-custom-name-input {
        width: 100%;
        font-size: 0.84rem;
        padding: 6px 10px;
        height: 34px;
    }
    .gpa-hours-warning {
        color: #f59e0b;
        font-size: 0.7rem;
        font-weight: 600;
        display: inline-block;
        margin-top: 2px;
    }

    /* Hours Input Container */
    .gpa-hours-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(0, 0, 0, 0.35);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 8px;
        padding: 2px 8px;
        height: 36px;
        box-sizing: border-box;
        gap: 6px;
        transition: all 0.2s ease;
    }
    .gpa-hours-wrapper:focus-within {
        border-color: rgba(56, 189, 248, 0.5);
        box-shadow: 0 0 0 2px rgba(56, 189, 248, 0.1);
    }
    .gpa-hours-wrapper.missing-warning {
        border-color: rgba(245, 158, 11, 0.5);
        background: rgba(245, 158, 11, 0.08);
    }
    .gpa-hours-input {
        width: 32px;
        background: transparent !important;
        border: none !important;
        text-align: center;
        color: #f8fafc;
        font-size: 0.9rem;
        font-weight: 700;
        padding: 0 !important;
        outline: none !important;
        box-shadow: none !important;
    }
    .gpa-hours-label {
        font-size: 0.72rem;
        color: #9ca3af;
        user-select: none;
        font-weight: 500;
    }

    /* Grade Select & Marks Input */
    .gpa-grade-wrapper {
        position: relative;
        display: inline-flex;
    }
    .gpa-grade-select {
        background: rgba(0, 0, 0, 0.45) !important;
        border: 1px solid rgba(255, 255, 255, 0.12) !important;
        border-radius: 8px !important;
        color: #38bdf8 !important;
        font-size: 0.88rem !important;
        font-weight: 700 !important;
        height: 36px !important;
        min-width: 72px !important;
        width: 100% !important;
        padding: 0 10px !important;
        text-align: center !important;
        cursor: pointer;
        appearance: auto !important;
        -webkit-appearance: auto !important;
        -moz-appearance: auto !important;
    }
    .gpa-grade-select option {
        background: #18181b;
        color: #f8fafc;
        font-weight: 600;
    }
    .gpa-marks-input {
        background: rgba(0, 0, 0, 0.45) !important;
        border: 1px solid rgba(255, 255, 255, 0.12) !important;
        border-radius: 8px !important;
        color: #38bdf8 !important;
        font-size: 0.88rem !important;
        font-weight: 700 !important;
        height: 36px !important;
        width: 72px !important;
        padding: 0 6px !important;
        text-align: center !important;
    }

    /* Delete Button */
    .gpa-btn-del {
        width: 30px;
        height: 30px;
        background: rgba(239, 68, 68, 0.08);
        border: 1px solid rgba(239, 68, 68, 0.2);
        color: #f87171;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        font-size: 1.1rem;
        transition: all 0.2s ease;
        padding: 0;
        line-height: 1;
        box-sizing: border-box;
    }
    .gpa-btn-del:hover {
        background: rgba(239, 68, 68, 0.2);
        border-color: rgba(239, 68, 68, 0.4);
        color: #fca5a5;
        transform: scale(1.05);
    }

    /* Action Buttons (Add Subject & Clear) */
    .gpa-actions-row {
        display: flex;
        gap: 10px;
        margin-top: 12px;
    }
    .gpa-btn-action {
        padding: 10px 14px;
        border-radius: 10px;
        font-size: 0.84rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s ease;
        font-family: inherit;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 6px;
        min-height: 40px;
        border: 1px solid transparent;
        box-sizing: border-box;
    }
    .gpa-btn-add {
        flex: 2;
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(255, 255, 255, 0.16);
        color: #e2e8f0;
    }
    .gpa-btn-add:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(255, 255, 255, 0.3);
        color: #ffffff;
    }
    .gpa-btn-clear {
        flex: 1;
        background: rgba(239, 68, 68, 0.06);
        border-color: rgba(239, 68, 68, 0.2);
        color: #f87171;
    }
    .gpa-btn-clear:hover {
        background: rgba(239, 68, 68, 0.14);
        border-color: rgba(239, 68, 68, 0.35);
        color: #fca5a5;
    }

    /* --------------------------------------------------
       5. TARGET GPA CALCULATOR CARD
       -------------------------------------------------- */
    .gpa-target-input-row {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 8px;
    }
    .gpa-target-label {
        font-size: 0.82rem;
        color: #cbd5e1;
        white-space: nowrap;
        font-weight: 500;
    }
    .gpa-target-input {
        flex: 1;
        padding: 8px 12px;
        height: 38px;
        font-size: 0.9rem;
        font-weight: 600;
        text-align: center;
    }
    .gpa-target-alert {
        margin-top: 10px;
        padding: 10px 12px;
        border-radius: 10px;
        font-size: 0.82rem;
        line-height: 1.45;
        text-align: center;
        animation: fadeIn 0.25s ease;
    }
    .gpa-target-alert.normal {
        background: rgba(56, 189, 248, 0.08);
        border: 1px solid rgba(56, 189, 248, 0.2);
        color: #e2e8f0;
    }
    .gpa-target-alert.success {
        background: rgba(34, 197, 94, 0.1);
        border: 1px solid rgba(34, 197, 94, 0.25);
        color: #4ade80;
    }
    .gpa-target-alert.danger {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.25);
        color: #f87171;
    }
    .gpa-target-alert.info {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #9ca3af;
    }

    /* --------------------------------------------------
       6. RESULTS SUMMARY BOX & STANDINGS
       -------------------------------------------------- */
    .results-box {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.04) 0%, rgba(255, 255, 255, 0.02) 100%);
        border: 1px solid rgba(255, 255, 255, 0.09);
        border-radius: 16px;
        padding: 16px 18px;
        margin-top: 14px;
        box-sizing: border-box;
        text-align: center;
    }
    .results-box-title {
        margin: 0 0 12px 0;
        font-size: 0.95rem;
        font-weight: 600;
        color: #cbd5e1;
        border-bottom: 1px solid rgba(255, 255, 255, 0.07);
        padding-bottom: 8px;
    }
    .results-grid {
        display: flex;
        justify-content: space-around;
        align-items: center;
        gap: 12px;
    }
    .result-stat-item {
        flex: 1;
        text-align: center;
    }
    .result-stat-label {
        display: block;
        font-size: 0.78rem;
        color: #9ca3af;
        margin-bottom: 4px;
        font-weight: 500;
    }
    .result-stat-val {
        font-size: 1.75rem;
        font-weight: 800;
        letter-spacing: -0.02em;
        line-height: 1.2;
    }
    .result-stat-val.sem {
        color: #38bdf8;
    }
    .result-stat-val.cum {
        color: #4ade80;
    }
    .results-divider {
        width: 1px;
        height: 42px;
        background: rgba(255, 255, 255, 0.1);
    }
    .gpa-standing-wrapper {
        margin-top: 12px;
        padding-top: 10px;
        border-top: 1px solid rgba(255, 255, 255, 0.06);
        display: flex;
        justify-content: center;
    }
    .gpa-standing-pill {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        font-size: 0.8rem;
        font-weight: 600;
        padding: 4px 12px;
        border-radius: 20px;
        transition: all 0.25s ease;
    }

    /* Webkit Scrollbar */
    .gpa-modal-content::-webkit-scrollbar { width: 5px; }
    .gpa-modal-content::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.2);
        border-radius: 4px;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(4px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>

<!-- Banner Trigger -->
<div class="gpa-banner" onclick="openGPAModal()">
    <div class="gpa-banner-text">
        <h3>حاسبة المعدل التراكمي والفصلي</h3>
        <p>احسب التقدير المتوقع ومعدل الهدف بالتفصيل</p>
    </div>
    <div class="gpa-banner-arrow">←</div>
</div>

<!-- Modal Overlay -->
<div class="gpa-modal-overlay" id="gpaModal" onclick="closeModalOnBgClick(event)">
    <div class="gpa-modal-content">
        <button type="button" class="gpa-close-btn" onclick="closeGPAModal()" title="إغلاق">×</button>
        
        <div class="gpa-modal-header">
            <h2 class="gpa-modal-title">حاسبة المعدل الأكاديمي</h2>
            <p id="gpa-modal-subtitle" class="gpa-modal-subtitle"></p>
        </div>
        
        <!-- Previous GPA Card -->
        <div class="gpa-card">
            <h3 class="gpa-card-title" style="color: #cbd5e1; font-size: 0.88rem; margin-bottom: 10px;">المعدل التراكمي السابق (اختياري)</h3>
            <div class="gpa-prev-grid">
                <div class="gpa-field-group">
                    <label class="gpa-field-label" for="prev-hours">الساعات السابقة</label>
                    <input type="number" id="prev-hours" class="gpa-field-input" min="0" oninput="calculateGPA(true)" placeholder="مثال: 35">
                </div>
                <div class="gpa-field-group">
                    <label class="gpa-field-label" for="prev-gpa">المعدل السابق (من 5.0)</label>
                    <input type="number" id="prev-gpa" class="gpa-field-input" min="0" max="5" step="0.01" oninput="calculateGPA(true)" placeholder="مثال: 4.50">
                </div>
            </div>
        </div>

        <!-- Current Term Subjects Card -->
        <div class="gpa-card">
            <div class="gpa-card-header">
                <h3 id="current-term-hours-text" class="gpa-card-title">
                    مواد الفصل الحالي
                    <span id="current-term-hours-badge" class="gpa-hours-badge">0 ساعة</span>
                </h3>
                <div class="gpa-toggle-pill">
                    <button type="button" id="gpa-mode-letter" class="gpa-toggle-btn active" onclick="setGradeMode('letter')">تقدير</button>
                    <button type="button" id="gpa-mode-marks" class="gpa-toggle-btn" onclick="setGradeMode('marks')">درجة / 100</button>
                </div>
            </div>
            
            <div id="subjects-list"></div>

            <div class="gpa-actions-row">
                <button type="button" class="gpa-btn-action gpa-btn-add" onclick="addCustomSubject()">
                    + إضافة مادة
                </button>
                <button type="button" class="gpa-btn-action gpa-btn-clear" onclick="clearGpaGrades()" title="تفريغ التقديرات والدرجات المدخلة">
                    تفريغ الدرجات
                </button>
            </div>
        </div>

        <!-- Target GPA Calculator Card -->
        <div class="gpa-card">
            <h3 class="gpa-card-title">حاسبة هدف المعدل</h3>
            <p style="margin: 4px 0 8px 0; font-size: 0.78rem; color: #9ca3af; line-height: 1.4;">
                أدخل المعدل التراكمي الذي تطمح للوصول إليه لمعرفة المعدل الفصلي والتقدير المطلوب في المواد الحالية.
            </p>
            <div class="gpa-target-input-row">
                <label class="gpa-target-label" for="target-gpa">المعدل المستهدف:</label>
                <input type="number" id="target-gpa" class="gpa-target-input" min="0" max="5" step="0.01" placeholder="مثال: 4.75" oninput="calculateGPA(true)">
            </div>
            <div id="target-gpa-result" style="display: none;"></div>
        </div>

        <!-- Results Summary Box -->
        <div class="results-box">
            <h3 class="results-box-title">النتائج المتوقعة</h3>
            <div class="results-grid">
                <div class="result-stat-item">
                    <span class="result-stat-label">المعدل الفصلي</span>
                    <strong id="semester-gpa" class="result-stat-val sem">0.00</strong>
                </div>
                <div class="results-divider"></div>
                <div class="result-stat-item">
                    <span class="result-stat-label">المعدل التراكمي</span>
                    <strong id="cumulative-gpa" class="result-stat-val cum">0.00</strong>
                </div>
            </div>
            <div class="gpa-standing-wrapper">
                <div id="gpa-standing-badge" class="gpa-standing-pill" style="background: rgba(255,255,255,0.05); color: #9ca3af;">
                    في انتظار إدخال التقديرات
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Global State for GPA Calculator
    let dentGpaSubjects = [];
    let dentGpaSelection = null;
    let dentGpaGradeMode = 'letter'; // 'letter' or 'marks'

    function parseLocalNum(val) {
        if (val === null || val === undefined) return 0;
        const s = String(val).replace(/[٠-٩]/g, d => "٠١٢٣٤٥٦٧٨٩".indexOf(d)).replace(/[^\d.-]/g, '');
        const num = parseFloat(s);
        return isNaN(num) ? 0 : num;
    }

    const dentGradePoints5 = {
        "": 0,
        "A+": 5.0,
        "A": 4.75,
        "B+": 4.5,
        "B": 4.0,
        "C+": 3.5,
        "C": 3.0,
        "D+": 2.5,
        "D": 2.0,
        "F": 0.0
    };

    function marksToGrade(marks) {
        const val = parseLocalNum(marks);
        if (val <= 0 && String(marks).trim() === '') return "";
        if (val >= 95) return "A+";
        if (val >= 90) return "A";
        if (val >= 85) return "B+";
        if (val >= 80) return "B";
        if (val >= 75) return "C+";
        if (val >= 70) return "C";
        if (val >= 65) return "D+";
        if (val >= 60) return "D";
        return "F";
    }

    function getAcademicStanding(gpa) {
        if (gpa >= 4.75) return { text: 'ممتاز مرتفع (A+)', color: '#4ade80', bg: 'rgba(74, 222, 128, 0.12)', border: 'rgba(74, 222, 128, 0.25)' };
        if (gpa >= 4.50) return { text: 'ممتاز (A)', color: '#38bdf8', bg: 'rgba(56, 189, 248, 0.12)', border: 'rgba(56, 189, 248, 0.25)' };
        if (gpa >= 4.00) return { text: 'جيد جداً مرتفع (B+)', color: '#818cf8', bg: 'rgba(129, 140, 248, 0.12)', border: 'rgba(129, 140, 248, 0.25)' };
        if (gpa >= 3.50) return { text: 'جيد جداً (B)', color: '#a78bfa', bg: 'rgba(167, 139, 250, 0.12)', border: 'rgba(167, 139, 250, 0.25)' };
        if (gpa >= 3.00) return { text: 'جيد مرتفع (C+)', color: '#fbbf24', bg: 'rgba(251, 191, 36, 0.12)', border: 'rgba(251, 191, 36, 0.25)' };
        if (gpa >= 2.50) return { text: 'جيد (C)', color: '#f59e0b', bg: 'rgba(245, 158, 11, 0.12)', border: 'rgba(245, 158, 11, 0.25)' };
        if (gpa >= 2.00) return { text: 'مقبول مرتفع (D+)', color: '#fb923c', bg: 'rgba(251, 146, 60, 0.12)', border: 'rgba(251, 146, 60, 0.25)' };
        if (gpa >= 1.75) return { text: 'مقبول (D)', color: '#f97316', bg: 'rgba(249, 115, 22, 0.12)', border: 'rgba(249, 115, 22, 0.25)' };
        if (gpa > 0) return { text: 'راسب (F)', color: '#f87171', bg: 'rgba(248, 113, 113, 0.12)', border: 'rgba(248, 113, 113, 0.25)' };
        return { text: 'في انتظار إدخال التقديرات', color: '#9ca3af', bg: 'rgba(255, 255, 255, 0.05)', border: 'rgba(255, 255, 255, 0.1)' };
    }

    // Modal Control
    function openGPAModal() {
        const modal = document.getElementById('gpaModal');
        if (modal) {
            if (modal.parentNode !== document.body) {
                document.body.appendChild(modal);
            }
            modal.style.setProperty('display', 'flex', 'important');
            void modal.offsetWidth;
            modal.classList.add('active');
            document.body.style.overflow = 'hidden';
            initDentGpaCalculator();
        }
    }

    function closeGPAModal() {
        const modal = document.getElementById('gpaModal');
        if (modal) {
            modal.classList.remove('active');
            document.body.style.overflow = '';
            setTimeout(function() {
                if (!modal.classList.contains('active')) {
                    modal.style.setProperty('display', 'none', 'important');
                }
            }, 250);
        }
    }

    function closeModalOnBgClick(e) {
        if (e.target.id === 'gpaModal') {
            closeGPAModal();
        }
    }

    function initDentGpaCalculator() {
        const rawSel = localStorage.getItem('dent2025_selection');
        if (rawSel) {
            try {
                dentGpaSelection = JSON.parse(rawSel);
            } catch (e) {
                dentGpaSelection = null;
            }
        } else {
            dentGpaSelection = null;
        }

        updateGpaHeaderTitle();
        loadDentGpaSubjects();
    }

    function updateGpaHeaderTitle() {
        const titleEl = document.getElementById('gpa-modal-subtitle');
        if (!titleEl) return;

        if (dentGpaSelection) {
            let specText = dentGpaSelection.specialty === 'dentistry' ? 'طب الأسنان' : (dentGpaSelection.specialty === 'medicine' ? 'الطب البشري' : 'التحضيري');
            let semText = `السنة ${dentGpaSelection.year} - الفصل ${dentGpaSelection.semester}`;
            if (dentGpaSelection.specialty === 'pre-med') {
                semText = `التحضيري - الفصل ${dentGpaSelection.semester}`;
            }
            titleEl.innerText = `${specText} (${semText})`;
        } else {
            titleEl.innerText = 'حاسبة المعدل الأكاديمي';
        }
    }

    function getStorageKey() {
        if (dentGpaSelection) {
            return `dent2025_gpa_${dentGpaSelection.specialty}_y${dentGpaSelection.year}_s${dentGpaSelection.semester}`;
        }
        return 'dent2025_simple_gpa';
    }

    function loadDentGpaSubjects() {
        let apiSubjects = [];

        if (dentGpaSelection) {
            const cacheKey = `dent2025_dashboard_data_${dentGpaSelection.specialty}_${dentGpaSelection.year}_${dentGpaSelection.semester}`;
            const cached = sessionStorage.getItem(cacheKey);
            if (cached) {
                try {
                    const parsed = JSON.parse(cached);
                    if (parsed.subjects) {
                        apiSubjects = parsed.subjects;
                    }
                } catch(e){}
            }
        }

        // If no cached subjects, attempt fetch from API
        if (dentGpaSelection) {
            if (apiSubjects.length === 0) {
                const apiBase = (window.location.pathname === '/dev' || window.location.pathname.startsWith('/dev/')) ? '/dev' : '';
                fetch(`${apiBase}/dent2025_api.php?action=data&specialty=${dentGpaSelection.specialty}&year=${dentGpaSelection.year}&semester=${dentGpaSelection.semester}`)
                    .then(res => res.json())
                    .then(resData => {
                        if (resData.success && resData.data && resData.data.subjects) {
                            apiSubjects = resData.data.subjects;
                            buildSubjectsList(apiSubjects);
                        } else {
                            buildSubjectsList([]);
                        }
                    })
                    .catch(err => {
                        buildSubjectsList([]);
                    });
                return;
            }
        }

        buildSubjectsList(apiSubjects);
    }

    function buildSubjectsList(apiSubjects) {
        dentGpaSubjects = [];

        // Check local storage for saved state
        const storageKey = getStorageKey();
        const savedRaw = localStorage.getItem(storageKey);
        let savedState = null;
        if (savedRaw) {
            try { savedState = JSON.parse(savedRaw); } catch(e){}
        }

        // Fallback demo subjects if no selection and no API
        if (apiSubjects.length === 0 && !dentGpaSelection) {
            apiSubjects = [
                { id: "od", name: "تشخيص الفم | Oral Diagnosis", hours: 2 },
                { id: "gp", name: "علم الأمراض العام | General Pathology", hours: 3 },
                { id: "abm", name: "مواد طب الأسنان المتقدمة | Biomaterials", hours: 2 },
                { id: "omi", name: "الأحياء الدقيقة | Microbiology", hours: 3 },
                { id: "dph", name: "الصحة العامة | Public Health", hours: 1 }
            ];
        }

        // Map API subjects to local state
        apiSubjects.forEach((sub, idx) => {
            let parsedHours = 0;
            if (typeof sub.hours === 'string') {
                const tMatch = sub.hours.match(/(\d+)\s*نظري/);
                const pMatch = sub.hours.match(/(\d+)\s*عملي/);
                let th = tMatch ? parseFloat(tMatch[1]) : 0;
                let pr = pMatch ? parseFloat(pMatch[1]) : 0;
                if (th > 0 || pr > 0) {
                    parsedHours = th + pr;
                } else {
                    parsedHours = parseFloat(sub.hours) || 0;
                }
            } else {
                parsedHours = parseFloat(sub.hours) || 0;
            }

            let isMissing = (isNaN(parsedHours) || parsedHours <= 0);
            if (isMissing) parsedHours = 0;

            let restoredHours = parsedHours;
            let restoredGrade = "";
            let restoredMarks = "";

            if (savedState && savedState.grades) {
                const savedItem = savedState.grades[sub.id || idx];
                if (savedItem) {
                    if (savedItem.grade !== undefined) restoredGrade = savedItem.grade;
                    if (savedItem.marks !== undefined) restoredMarks = savedItem.marks;
                    if (savedItem.hours !== undefined) {
                        const sh = parseFloat(savedItem.hours);
                        if (!isNaN(sh) && sh > 0) restoredHours = sh;
                    }
                }
            }

            dentGpaSubjects.push({
                id: sub.id || `sub_${idx}`,
                name: sub.name || sub.nameAr || `مادة ${idx + 1}`,
                hours: restoredHours,
                isMissingHours: isMissing,
                isCustom: false,
                grade: restoredGrade,
                marks: restoredMarks
            });
        });

        // Restore custom subjects if saved
        if (savedState) {
            if (savedState.customSubjects && Array.isArray(savedState.customSubjects)) {
                savedState.customSubjects.forEach(cSub => {
                    dentGpaSubjects.push({
                        id: cSub.id || `custom_${Date.now()}_${Math.random()}`,
                        name: cSub.name || 'مادة إضافية',
                        hours: parseFloat(cSub.hours) || 2,
                        isMissingHours: false,
                        isCustom: true,
                        grade: cSub.grade || '',
                        marks: cSub.marks || ''
                    });
                });
            }

            if (savedState.prevHours) {
                const phInput = document.getElementById('prev-hours');
                if (phInput) phInput.value = savedState.prevHours;
            }
            if (savedState.prevGpa) {
                const pgInput = document.getElementById('prev-gpa');
                if (pgInput) pgInput.value = savedState.prevGpa;
            }
            if (savedState.targetGpa) {
                const tgInput = document.getElementById('target-gpa');
                if (tgInput) tgInput.value = savedState.targetGpa;
            }
        }

        renderSubjectsTable();
        calculateGPA(false);
    }

    function renderSubjectsTable() {
        const container = document.getElementById('subjects-list');
        if (!container) return;

        if (dentGpaSubjects.length === 0) {
            container.innerHTML = `
                <div style="text-align: center; color: #9ca3af; padding: 22px 14px; font-size: 0.85rem; background: rgba(255,255,255,0.02); border-radius: 12px; border: 1px dashed rgba(255,255,255,0.1);">
                    لا توجد مواد مسجلة لهذا الفصل حتى الآن.<br>
                    يمكنك إضافة مواد يدوياً باستخدام الزر أدناه.
                </div>
            `;
            return;
        }

        let html = '';
        dentGpaSubjects.forEach((sub, idx) => {
            let hasWarning = false;
            if (sub.isMissingHours && sub.hours <= 0) {
                hasWarning = true;
            }
            const warningBadge = hasWarning ? `<span class="gpa-hours-warning">حدد الساعات</span>` : '';
            const hoursWrapperClass = hasWarning ? 'gpa-hours-wrapper missing-warning' : 'gpa-hours-wrapper';

            let gradeInputHtml = '';
            if (dentGpaGradeMode === 'letter') {
                gradeInputHtml = `
                    <select class="gpa-grade-select" onchange="updateSubjectGrade(${idx}, this.value)" aria-label="تقدير المادة">
                        <option value="" ${sub.grade === '' ? 'selected' : ''}>--</option>
                        <option value="A+" ${sub.grade === 'A+' ? 'selected' : ''}>A+</option>
                        <option value="A" ${sub.grade === 'A' ? 'selected' : ''}>A</option>
                        <option value="B+" ${sub.grade === 'B+' ? 'selected' : ''}>B+</option>
                        <option value="B" ${sub.grade === 'B' ? 'selected' : ''}>B</option>
                        <option value="C+" ${sub.grade === 'C+' ? 'selected' : ''}>C+</option>
                        <option value="C" ${sub.grade === 'C' ? 'selected' : ''}>C</option>
                        <option value="D+" ${sub.grade === 'D+' ? 'selected' : ''}>D+</option>
                        <option value="D" ${sub.grade === 'D' ? 'selected' : ''}>D</option>
                        <option value="F" ${sub.grade === 'F' ? 'selected' : ''}>F</option>
                    </select>
                `;
            } else {
                gradeInputHtml = `
                    <input type="number" class="gpa-marks-input" min="0" max="100" value="${sub.marks !== undefined ? sub.marks : ''}" placeholder="/100" oninput="updateSubjectMarks(${idx}, this.value)" aria-label="درجة المادة من 100">
                `;
            }

            const deleteBtn = `<button type="button" class="gpa-btn-del" onclick="removeSubjectRow(${idx})" title="حذف المادة" aria-label="حذف المادة">×</button>`;

            const safeSubName = String(sub.name || '').replace(/[&<>"']/g, m => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[m]));

            html += `
                <div class="gpa-subject-item">
                    <div class="gpa-subject-top-row">
                        <div class="gpa-subject-info">
                            ${sub.isCustom ? `<input type="text" value="${safeSubName}" onchange="updateSubjectName(${idx}, this.value)" class="gpa-custom-name-input" placeholder="اسم المادة">` : `<h4 class="gpa-subject-name" title="${safeSubName}" dir="auto">${safeSubName}</h4>`}
                            ${warningBadge}
                        </div>
                        <div class="gpa-mobile-del-wrapper">
                            ${deleteBtn}
                        </div>
                    </div>
                    <div class="gpa-subject-controls">
                        <div class="${hoursWrapperClass}" title="عدد الساعات المعتمدة">
                            <input type="number" min="1" max="12" class="gpa-hours-input" value="${sub.hours > 0 ? sub.hours : ''}" placeholder="0" oninput="updateSubjectHours(${idx}, this.value)" aria-label="ساعات المادة">
                            <span class="gpa-hours-label">ساعات</span>
                        </div>
                        <div class="gpa-grade-wrapper">
                            ${gradeInputHtml}
                        </div>
                        <div class="gpa-desktop-del-wrapper">
                            ${deleteBtn}
                        </div>
                    </div>
                </div>
            `;
        });

        container.innerHTML = html;
    }

    function setGradeMode(mode) {
        dentGpaGradeMode = mode;
        const btnLetter = document.getElementById('gpa-mode-letter');
        const btnMarks = document.getElementById('gpa-mode-marks');
        if (btnLetter) {
            if (mode === 'letter') btnLetter.classList.add('active');
            else btnLetter.classList.remove('active');
        }
        if (btnMarks) {
            if (mode === 'marks') btnMarks.classList.add('active');
            else btnMarks.classList.remove('active');
        }
        renderSubjectsTable();
    }

    function updateSubjectHours(index, val) {
        const parsed = parseLocalNum(val);
        dentGpaSubjects[index].hours = parsed;
        calculateGPA(true);
    }

    function updateSubjectGrade(index, val) {
        dentGpaSubjects[index].grade = val;
        calculateGPA(true);
    }

    function updateSubjectMarks(index, val) {
        dentGpaSubjects[index].marks = val;
        dentGpaSubjects[index].grade = marksToGrade(val);
        calculateGPA(true);
    }

    function updateSubjectName(index, val) {
        dentGpaSubjects[index].name = val || 'مادة جديدة';
        saveState();
    }

    function addCustomSubject() {
        dentGpaSubjects.push({
            id: `custom_${Date.now()}`,
            name: 'مادة جديدة',
            hours: 2,
            isMissingHours: false,
            isCustom: true,
            grade: '',
            marks: ''
        });
        renderSubjectsTable();
        calculateGPA(true);
    }

    function removeSubjectRow(index) {
        dentGpaSubjects.splice(index, 1);
        renderSubjectsTable();
        calculateGPA(true);
    }

    function clearGpaGrades() {
        dentGpaSubjects.forEach(sub => {
            sub.grade = '';
            sub.marks = '';
        });
        renderSubjectsTable();
        calculateGPA(true);
    }

    function calculateGPA(save = false) {
        let semesterPoints = 0;
        let semesterCredits = 0;

        dentGpaSubjects.forEach(sub => {
            const hrs = parseLocalNum(sub.hours);
            const pts = dentGradePoints5[sub.grade] !== undefined ? dentGradePoints5[sub.grade] : 0;
            if (hrs > 0 && sub.grade) {
                semesterPoints += (pts * hrs);
                semesterCredits += hrs;
            }
        });

        // Compute Semester Total Hours
        let totalCurrentTermHours = 0;
        dentGpaSubjects.forEach(sub => {
            const h = parseLocalNum(sub.hours);
            if (h > 0) totalCurrentTermHours += h;
        });

        const currentTermBadge = document.getElementById('current-term-hours-badge');
        if (currentTermBadge) {
            currentTermBadge.innerText = `${totalCurrentTermHours} ساعة`;
        }

        const semesterGPA = semesterCredits > 0 ? (semesterPoints / semesterCredits) : 0;
        const semGpaEl = document.getElementById('semester-gpa');
        if (semGpaEl) semGpaEl.innerText = semesterGPA.toFixed(2);

        const prevHoursEl = document.getElementById('prev-hours');
        const prevGpaEl = document.getElementById('prev-gpa');
        const prevHours = prevHoursEl ? parseLocalNum(prevHoursEl.value) : 0;
        const prevGPA = prevGpaEl ? parseLocalNum(prevGpaEl.value) : 0;

        let cumulativeGPA = 0;
        let totalCreditsAll = prevHours + semesterCredits;

        if (prevHours > 0 || semesterCredits > 0) {
            const totalPoints = (prevHours * prevGPA) + semesterPoints;
            if (totalCreditsAll > 0) {
                cumulativeGPA = totalPoints / totalCreditsAll;
            }
            const cumGpaEl = document.getElementById('cumulative-gpa');
            if (cumGpaEl) cumGpaEl.innerText = cumulativeGPA.toFixed(2);
        } else {
            const cumGpaEl = document.getElementById('cumulative-gpa');
            if (cumGpaEl) cumGpaEl.innerText = "0.00";
        }

        // Update Academic Standing Badge
        const standingEl = document.getElementById('gpa-standing-badge');
        if (standingEl) {
            const relevantGPA = cumulativeGPA > 0 ? cumulativeGPA : semesterGPA;
            const standing = getAcademicStanding(relevantGPA);
            standingEl.innerText = standing.text;
            standingEl.style.color = standing.color;
            standingEl.style.backgroundColor = standing.bg;
            standingEl.style.border = `1px solid ${standing.border}`;
        }

        calculateTargetGpaEstimate(prevHours, prevGPA, totalCurrentTermHours, cumulativeGPA);

        if (save) saveState();
    }

    function calculateTargetGpaEstimate(prevHours, prevGPA, termHours, currentCumGpa) {
        const targetInput = document.getElementById('target-gpa');
        const resultEl = document.getElementById('target-gpa-result');
        if (!targetInput || !resultEl) return;

        const targetGpa = parseLocalNum(targetInput.value);
        if (isNaN(targetGpa) || targetGpa <= 0) {
            resultEl.innerHTML = '';
            resultEl.style.display = 'none';
            return;
        }

        resultEl.style.display = 'block';

        if (termHours <= 0) {
            resultEl.innerHTML = '<div class="gpa-target-alert info">الرجاء تحديد ساعات المواد للفصل الحالي أولاً.</div>';
            return;
        }

        const totalHours = prevHours + termHours;
        const reqSemGPA = (targetGpa * totalHours - (prevHours * prevGPA)) / termHours;

        if (reqSemGPA > 5.0) {
            resultEl.innerHTML = `<div class="gpa-target-alert danger">تحتاج إلى معدل فصلي <strong>${reqSemGPA.toFixed(2)}</strong> (أعلى من 5.0 — غير ممكن في فصل واحد).</div>`;
        } else if (reqSemGPA <= 0) {
            resultEl.innerHTML = `<div class="gpa-target-alert success">🎉 هدفك محقق بالفعل بفضل معدلك السابق!</div>`;
        } else {
            let letterHint = "A+";
            if (reqSemGPA <= 2.0) letterHint = "D";
            else if (reqSemGPA <= 2.5) letterHint = "D+";
            else if (reqSemGPA <= 3.0) letterHint = "C";
            else if (reqSemGPA <= 3.5) letterHint = "C+";
            else if (reqSemGPA <= 4.0) letterHint = "B";
            else if (reqSemGPA <= 4.5) letterHint = "B+";
            else if (reqSemGPA <= 4.75) letterHint = "A";

            resultEl.innerHTML = `<div class="gpa-target-alert normal">تحتاج إلى معدل فصلي <strong style="color: #38bdf8; font-size: 1.02rem;">${reqSemGPA.toFixed(2)}</strong> (متوسط تقدير <strong>${letterHint}</strong>) لتحقيق هدفك.</div>`;
        }
    }

    function saveState() {
        const prevHoursEl = document.getElementById('prev-hours');
        const prevGpaEl = document.getElementById('prev-gpa');
        const targetGpaEl = document.getElementById('target-gpa');

        const state = {
            prevHours: prevHoursEl ? prevHoursEl.value : '',
            prevGpa: prevGpaEl ? prevGpaEl.value : '',
            targetGpa: targetGpaEl ? targetGpaEl.value : '',
            grades: {},
            customSubjects: []
        };

        dentGpaSubjects.forEach(sub => {
            if (sub.isCustom) {
                state.customSubjects.push(sub);
            } else {
                state.grades[sub.id] = {
                    grade: sub.grade,
                    marks: sub.marks,
                    hours: sub.hours
                };
            }
        });

        const storageKey = getStorageKey();
        try {
            localStorage.setItem(storageKey, JSON.stringify(state));
        } catch(e){}
    }
</script>
